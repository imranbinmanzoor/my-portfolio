// Shared book overview for every published class. Authored mathematics stays separate.
const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderOverview(book){
  const live=book.units.filter(u=>u.available);
  return `<header class="library-top"><nav class="crumb" aria-label="Breadcrumb"><ol><li><a href="/"><span class="crumb-home-icon" aria-hidden="true">⌂</span><span class="crumb-home-label">Home</span></a></li><li><a href="/solutions/">Mathematics</a></li><li aria-current="page">Class ${book.class}</li></ol></nav></header>
  <div class="book-overview-hero"><div><p class="book-kicker">${escape(book.board)}</p><h1>Class ${book.class}<br><span> Mathematics</span></h1><p class="book-overview-lede">Learn the ideas. Follow the reasoning.<br> Build confidence through practice.</p><p class="book-coverage">${live.length} of ${book.units.length} units available · Free to read</p></div><div class="overview-cover" aria-hidden="true"><span>MATHEMATICS</span><strong>${String(book.class).padStart(2,'0')}</strong><small>Worked solutions<br>&amp; practice</small></div></div>
  <section class="book-directory" aria-labelledby="contents-title"><div class="contents-heading"><div><p class="book-kicker">Your starting point</p><h2 id="contents-title">Explore the book</h2></div><div class="unit-search"><label for="unit-filter" class="sr-only">Find a unit by name or number</label><input id="unit-filter" type="search" placeholder="Find a unit" autocomplete="off"><button type="button" id="unit-filter-clear" aria-label="Clear unit search" hidden>×</button></div></div><div class="unit-directory" id="units">${book.units.map(u=>u.available?`<article class="unit-feature" data-unit-search="${escape('Unit '+u.n+' '+u.title)}"><span class="feature-index">${String(u.n).padStart(2,'0')}</span><div class="feature-content"><p class="unit-availability">Ready to study</p><h3>${escape(u.title)}</h3><p>${u.exercises} exercises · Review questions · Practice</p><div class="feature-links"><a class="book-button" href="${u.href}"><span class="btn__inner"><span class="btn__text">Start reading</span></span></a><a class="feature-practice" href="${u.practice}"><span class="btn__inner"><span class="btn__text">Build a practice paper</span></span></a></div></div></article>`:`<article class="unit-row" data-unit-search="${escape('Unit '+u.n+' '+u.title)}"><span class="unit-index">${String(u.n).padStart(2,'0')}</span><div><h3>${escape(u.title)}</h3><p>In preparation</p></div></article>`).join('')}</div><p class="unit-empty" id="unit-empty" hidden>No unit matches your search. Try a unit number or a shorter name.</p><p class="sr-only" id="unit-filter-status" role="status" aria-live="polite"></p><p class="book-edition-note">${escape(book.edition)}. This independent library is growing; units marked in preparation do not yet have published solutions.</p></section>`;
}
